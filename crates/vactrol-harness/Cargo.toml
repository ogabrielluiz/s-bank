[package]
name = "vactrol-harness"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Headless signal generation and golden management for the vactrol LPG core"

[dependencies]
vactrol-core = { path = "../vactrol-core" }
serde_json = "1"
